<div class="form-check">
    <input class="form-check-input" type="radio" name="{{name}}" id="{{el_id}}" value="{{value}}" {{is_checked}}>
    <label class="form-check-label">
        {{label}}
    </label>
</div>
