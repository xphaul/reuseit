<div class="form-group">
    <label>{{label}}</label>
    <input id="{{el_id}}" name="{{name}}" value="{{value}}" type="email" class="form-control" required>
    {{>formHidden name=name_id value=value_id}}
</div>
